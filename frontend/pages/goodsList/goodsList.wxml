<van-dropdown-menu active-color="#1ae6e6">
	<van-dropdown-item value="{{ value1 }}" options="{{ option1 }}" />
	<van-dropdown-item value="{{ value2 }}" options="{{ option2 }}" />
	<van-dropdown-item value="{{ value3 }}" options="{{ option3 }}" />
</van-dropdown-menu>

<van-card price="10.00" desc="描述信息" title="商品标题" thumb="../../image/postphoto2.png" thumb-mode="aspectFill" custom-class="goods-card" price-class="price" bindtap="viewGoods">
	<view slot="footer">
		<van-button size="mini">购买</van-button>
	</view>
</van-card>